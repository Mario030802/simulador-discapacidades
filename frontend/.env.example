# URL base del backend. Copia este archivo a .env y ajusta si el backend no está en localhost:3000.
# Si no defines nada, el front usa http://localhost:3000 por defecto.
VITE_API_URL=http://localhost:3000
